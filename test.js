import {
	Program
} from "./index.js";

// How should we handle exceptions? keywords are good.
main = Program({
	// special name

	// Actually, our route is more based off of what arguments
	// are passed... basically that needs to get matched...
	// word. WORD. This makes some sense!
	// So, we change this to a list of... endpoints? Maybe.
	"index": () => {
		//
	},
});